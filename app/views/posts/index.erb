Hackzor newz
<a href="/post/new">new</a>
<ol>
  <% @posts.each do |post| %>
    <div id="post<%= post.id %>">
      <li>
        <h5><a href="<%= post.url %>"><%= post.title %></a> <a href="">base url</a></h5>
      </li>
      <span>points.count by user.name <a href="/post/<%= post.id %>"><%= post.created_at %></a> comments.count</span>
    </div>
  <% end %>
</ol>
